# Environment variables for markdown-vault
# Copy this file to .env and update with your values

# ==============================================================================
# SERVER VARIABLES (used by markdown-vault server)
# ==============================================================================

# API Key (will be auto-generated if not set)
MARKDOWN_VAULT_API_KEY=

# Vault path
MARKDOWN_VAULT_PATH=/path/to/your/vault

# Server configuration
MARKDOWN_VAULT_HOST=127.0.0.1
MARKDOWN_VAULT_PORT=27123

# Logging level (DEBUG, INFO, WARNING, ERROR)
MARKDOWN_VAULT_LOG_LEVEL=INFO

# ==============================================================================
# CLIENT VARIABLES (used by MCP clients connecting TO markdown-vault server)
# ==============================================================================
# Note: These are NOT used by the markdown-vault server itself.
# MCP clients (like with-context-mcp) use these to connect to our server.

# Recommended (white-label naming for new integrations)
# VAULT_API_URL=https://127.0.0.1:27123
# VAULT_API_KEY=your-api-key-here
# VAULT_NAME=your_vault_name

# Legacy (for compatibility with Obsidian-based MCP clients)
# OBSIDIAN_API_URL=https://127.0.0.1:27123
# OBSIDIAN_API_KEY=your-api-key-here
# OBSIDIAN_VAULT=your_vault_name
